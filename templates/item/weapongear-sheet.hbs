{{#> item_layout_partial container=true }}

<div class="grid grid-fixed-row grid-3col">
    <div class="resource" id="weapongear-qty">
        <label for="system.quantity" class="resource-label">Quantity</label>
        <input type="number" id="system.quantity" name="system.quantity" value="{{ idata.quantity }}" data-dtype="Number" />
    </div>
    <div class="resource" id="weapongear-weight">
        <label for="system.weight" class="resource-label">Weight</label>
        <input type="number" id="system.weight" name="system.weight" value="{{ idata.weight }}" data-dtype="Number"
        {{#unless hasRwPermission}}disabled{{/unless}} />
    </div>
    <div class="resource" id="weapongear-value">
        <label for="system.value" class="resource-label">Value</label>
        {{#if hasRwPermission}}
        <input type="number" id="system.value" name="system.value" value="{{ idata.value }}" data-dtype="Number" />
        {{else}}
        {{#if idata.unknownValue}}
        <input type="text" id="system.value" name="system.value" value="Unknown" data-dtype="String" disabled />
        {{else}}
        <input type="number" id="system.value" name="system.value" value="{{ idata.value }}" data-dtype="Number" disabled />
        {{/if}}
        {{/if}}
    </div>
</div>
<div class="grid grid-fixed-row grid-3col">
    <div class="resource" id="weapongear-quality">
        <label for="system.weaponQuality" class="resource-label">Weapon Quality</label>
        <input type="number" id="system.weaponQuality" name="system.weaponQuality" value="{{ idata.weaponQuality }}" data-dtype="Number"
        {{#unless hasRwPermission}}disabled{{/unless}} />
    </div>
    <div class="resource" id="weapongear-attack">
        <label for="system.attack" class="resource-label">Attack</label>
        <input type="number" id="system.attack" name="system.attack" value="{{ idata.attack }}" data-dtype="Number"
        {{#unless hasRwPermission}}disabled{{/unless}} />
    </div>
    <div class="resource" id="weapongear-defense">
        <label for="system.defense" class="resource-label">Defense</label>
        <input type="number" id="system.defense" name="system.defense" value="{{ idata.defense }}" data-dtype="Number"
        {{#unless hasRwPermission}}disabled{{/unless}} />
    </div>
</div>
<div class="grid grid-fixed-row grid-3col">
    <div class="resource" id="weapongear-wq-mod">
        <label for="system.wqModifier" class="resource-label">WQ Modifier</label>
        <input type="number" id="system.wqModifier" name="system.wqModifier" value="{{ idata.wqModifier }}" data-dtype="Number"
        {{#unless hasRwPermission}}disabled{{/unless}} />
    </div>
    <div class="resource" id="weapongear-attack-mod">
        <label for="system.attackModifier" class="resource-label">Attack Modifier</label>
        <input type="number" id="system.attackModifier" name="system.attackModifier" value="{{ idata.attackModifier }}" data-dtype="Number"
        {{#unless hasRwPermission}}disabled{{/unless}} />
    </div>
</div>
<div class="grid grid-fixed-row grid-3col">
    <div class="resource" id="weapongear-blunt">
        <label for="system.blunt" class="resource-label">Blunt</label>
        <input type="number" id="system.blunt" name="system.blunt" value="{{ idata.blunt }}" data-dtype="Number"
        {{#unless hasRwPermission}}disabled{{/unless}} />
    </div>
    <div class="resource" id="weapongear-edged">
        <label for="system.edged" class="resource-label">Edged</label>
        <input type="number" id="system.edged" name="system.edged" value="{{ idata.edged }}" data-dtype="Number"
        {{#unless hasRwPermission}}disabled{{/unless}} />
    </div>
    <div class="resource" id="weapongear-piercing">
        <label for="system.piercing" class="resource-label">Piercing</label>
        <input type="number" id="system.piercing" name="system.piercing" value="{{ idata.piercing }}" data-dtype="Number"
        {{#unless hasRwPermission}}disabled{{/unless}} />
    </div>
</div>
<div class="grid grid-fixed-row grid-3col">
    <div class="resource" id="weapongear-assocskill">
        <label for="system.assocSkill" class="resource-label">Associated Skill</label>
        {{#if hasCombatSkills}}
        <select class="resource-value" id="system.assocSkill" name="system.assocSkill" data-dtype="String">
            {{selectOptions combatSkills selected=idata.assocSkill valueAttr="key" labelAttr="key"}}
        </select>
        {{else}}
        <input type="text" id="system.assocSkill" name="system.assocSkill" value="{{ idata.assocSkill }}" data-dtype="String" />
        {{/if}}
    </div>
</div>
{{> item_standard_partial}}
{{> item_unknown_value_partial}}
{{> item_artifact_partial}}

{{/item_layout_partial}}
