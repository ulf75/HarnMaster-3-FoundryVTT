{{#if isEsotericCombat}}
{{#if isGM}}
<div class="resource-block">
    <div class="resource">
        <label for="system.isEquipped" class="resource-label">Attack Option</label>
        <span class="resource-span">
            <input type="checkbox" id="system.isEquipped" name="system.isEquipped" {{ checked idata.isEquipped }} />
        </span>
    </div>
</div>
{{/if}}
{{/if}}
