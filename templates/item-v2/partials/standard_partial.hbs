<div class="resource-block">
    {{#if (or iproxy.source (and hasRwPermission editable))}}
    <div class="grid grid-fixed-row grid-1col">
        <div class="resource">
            <label
                for="system.source"
                class="resource-label">
                Source
            </label>
            <input class="value" type="text" id="system.source" name="system.source" value="{{ iproxy.source }}" data-dtype="String"
            {{#unless (and hasRwPermission editable)}}disabled{{/unless}} />
        </div>
    </div>
    {{/if}}
    {{#if (or iproxy.notes editable)}}
    <div class="grid grid-fixed-row grid-1col">
        <div class="resource">
            <label
                for="system.notes"
                class="resource-label">
                Note
            </label>
            <input class="value" type="text" id="system.notes" name="system.notes" value="{{ iproxy.notes }}" data-dtype="String"
            {{#unless editable}}disabled{{/unless}} />
        </div>
    </div>
    {{/if}}
</div>
